<!DOCTYPE html> 
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>RunExpert - Sua Evolução na Corrida</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700;800&family=Open+Sans:wght@400;600&display=swap" rel="stylesheet">
  <style>
    :root {
      --primary: #00b383;
      --primary-dark: #008c68;
      --dark: #000;
      --light: #fff;
      --gray: #ccc; /* ficou mais claro para aparecer no fundo escuro */
      --transition: all 0.3s ease;
      --shadow: 0 5px 15px rgba(0,0,0,0.2);
      --radius: 12px;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Open Sans', sans-serif;
      background: #111; /* fundo mais escuro */
      color: var(--light); /* textos claros */
      line-height: 1.6;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    .main-container {
      flex: 1;
      display: flex;
      flex-direction: column;
    }

    .hero-section {
      flex: 1;
      padding: 60px 40px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }

    .hero-content {
      max-width: 1200px;
      width: 100%;
      display: flex;
      flex-direction: column;
      gap: 40px;
    }

    @media (min-width: 992px) {
      .hero-content {
        flex-direction: row;
        align-items: center;
      }
    }

    /* Área reservada para imagens */
    .hero-graphic {
      flex: 1;
      display: flex;
      justify-content: center;
      align-items: center;
      position: relative;
    }

    .graphic-container {
      position: relative;
      width: 100%;
      max-width: 500px;
      height: 350px;
    }

    /* Imagem de decoração */
    .decoracao {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
      border-radius: var(--radius);
      opacity: 0.81; /* mais discreta no fundo escuro */
    }

    /* Imagem do corredor por cima */
    .corredor {
      position: absolute;
      bottom: 0;
      left: 50%;
      transform: translateX(-50%);
      max-width: 55%; /* diminuída */
      height: auto;
    }

    .hero-info {
      flex: 1;
      display: flex;
      flex-direction: column;
      gap: 25px;
      padding: 20px 0;
    }

    .training-badge {
      color: var(--primary);
      font-size: 1.1rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 2px;
      display: inline-block;
      padding: 8px 15px;
      background: rgba(0, 179, 131, 0.1);
      border-radius: 30px;
      align-self: flex-start;
      border: 1px solid rgba(0, 179, 131, 0.3);
    }

    .main-heading {
      font-family: 'Montserrat', sans-serif;
      font-size: 3.2rem;
      line-height: 1.2;
      font-weight: 800;
      color: var(--light);
    }

    .main-heading .highlight {
      color: var(--primary);
      position: relative;
      display: inline-block;
    }

    .divider {
      width: 100px;
      height: 4px;
      background: linear-gradient(90deg, var(--primary), #80d4b7);
      border-radius: 2px;
      margin: 15px 0;
    }

    .description {
      font-size: 1.2rem;
      color: var(--gray);
      max-width: 550px;
      line-height: 1.8;
    }

    .cta-button {
      display: inline-flex;
      align-items: center;
      gap: 12px;
      background: linear-gradient(90deg, var(--primary), var(--primary-dark));
      color: var(--light);
      padding: 18px 35px;
      border-radius: 50px;
      text-decoration: none;
      font-weight: 700;
      font-size: 1.1rem;
      transition: var(--transition);
      margin-top: 15px;
      box-shadow: var(--shadow);
      align-self: flex-start;
      border: none;
      cursor: pointer;
      position: relative;
      overflow: hidden;
    }

    .cta-button:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 20px rgba(0, 179, 131, 0.4);
    }

    .cta-button i {
      transition: transform 0.3s ease;
    }

    .cta-button:hover i {
      transform: translateX(5px);
    }

    .features {
      display: flex;
      flex-wrap: wrap;
      gap: 15px;
      margin-top: 20px;
    }

    .feature {
      display: flex;
      align-items: center;
      gap: 10px;
      background: rgba(255,255,255,0.05);
      padding: 12px 20px;
      border-radius: 30px;
      font-size: 0.9rem;
      border: 1px solid rgba(255,255,255,0.1);
      color: var(--light);
    }

    .feature i {
      color: var(--primary);
      font-size: 1.1rem;
    }

    @media (max-width: 991px) {
      .main-heading {
        font-size: 2.5rem;
      }
    }

    @media (max-width: 768px) {
      .hero-section {
        padding: 40px 20px;
      }

      .main-heading {
        font-size: 2rem;
      }

      .description {
        font-size: 1rem;
      }

      .cta-button {
        padding: 15px 25px;
        font-size: 1rem;
      }

      .features {
        flex-direction: column;
      }
    }
  </style>
</head>
<body>
  <div class="main-container">
    <section class="hero-section">
      <div class="hero-content">
        <!-- ÁREA DE IMAGENS -->
        <div class="hero-graphic">
          <div class="graphic-container">
            <!-- Coloque a imagem de decoração -->
            <img src="/images/decor.png" alt="Decoração" class="decoracao">
            <!-- Coloque a imagem sua correndo -->
            <img src="/images/correndo.png" alt="Eu correndo" class="corredor">
          </div>
        </div>

        <!-- TEXTO -->
        <div class="hero-info">
          <span class="training-badge">Treinamento de corrida online ou presencial</span>
          <h1 class="main-heading">
            <span>Sua </span>
            <span class="highlight">evolução</span>
            <span> na corrida é aqui</span>
          </h1>
          <div class="divider"></div>
          <p class="description">
            Treine na esteira, parque, rua ou onde preferir, tudo com a orientação de um coach especializado em treinamento de corrida.
          </p>
          <button class="cta-button">
            Comece agora
            <i class="fas fa-arrow-right"></i>
          </button>
          <div class="features">
            <div class="feature">
              <i class="fas fa-check-circle"></i>
              <span>Planilhas personalizadas</span>
            </div>
            <div class="feature">
              <i class="fas fa-check-circle"></i>
              <span>Acompanhamento especializado</span>
            </div>
            <div class="feature">
              <i class="fas fa-check-circle"></i>
              <span>Resultados garantidos</span>
            </div>
          </div>
        </div>
      </div>
    </section>
  </div>
</body>
</html>
